import { Box, Typography } from '@mui/material';

export default function CountUpView() {
  return (
    <Box sx={{ p: 3, textAlign: 'center' }}>
      <Typography variant="h4">Count Up Component</Typography>
      <Typography>This component is currently unavailable.</Typography>
    </Box>
  );
}
